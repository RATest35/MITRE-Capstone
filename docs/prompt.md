ノード数が数百万くらいある巨大グラフであるノードが与えられた時、そのノードのcomposite scoreをデータセットから予測するGNNを作りたい。

このプロジェクトの目的は、与えられたノードがどのくらいのダウンリスクを持っていて、そのノードがダウンするとどの程度の影響があるかを推定することです。
どのようなモデルを作るべきか、最も良いモデルをデータセットの特性から決めてください。前提として、page rank, weighted betweennessは本番環境では与えられません。